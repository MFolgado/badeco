<h4>Funcionários</h4><hr>

  <table class="table ">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @employees.each do |employee| %>
        <tr>
          <td><%= employee.name %></td>    
          <td><%= link_to t(:edit), edit_employee_path(employee) %></td>
          <td>
            <%= link_to t(:destroy), employee, method: :delete, data: { :confirm => t('.confirmation') } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
        
<div class="row">
  <div class="col-md-9">
    <div class="digg_pagination">
      <%= will_paginate @employees, :page_links => false %>
    </div>
  </div>
</div>
